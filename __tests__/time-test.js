const {times} = require('../times')
describe('times',()=>{
    test('Invokes the iteratee n times returning an array of the results',()=>{
        const a = (3)
        const b = [] 
        cons
        const c = (el){
            b.push(ele) =>  {

            }
        }

        })

    
})   
    
        expect(b).toBe([0, 0, 0])
    